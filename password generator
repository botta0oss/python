#simple fast script to create a random password (numbers and letters) between 4 and 8 digits

import string
from random import *

# variable to store the usable characters
characters = string.ascii_letters + string.digits

#randomizing choice form the characters variable an storing them in password
password =  "".join(choice(characters) for x in range(randint(4, 8)))

#print password
print (password)

